{
  "name": "tool-bg-remover",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosting build tooling for tool-bg-remover (the site itself is plain static files).",
  "type": "module",
  "scripts": {
    "vendor": "node scripts/vendor.mjs"
  },
  "devDependencies": {
    "@imgly/background-removal": "1.5.5",
    "esbuild": "^0.24.0"
  }
}
